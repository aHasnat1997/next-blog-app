import Hero from '@/components/home/Hero';
import { Button } from '@/components/ui/button';
import React from 'react';

const page = () => {
  return (
    <main>
      <Hero />
    </main>
  );
};

export default page;